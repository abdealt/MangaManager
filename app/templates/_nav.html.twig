<nav class="navbar navbar-expand-lg navbar-light" style="background: #2D2D2D; height: 70px;">
    <div class="container-fluid">
        <!-- Liens à gauche -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link text-white fw-semibold" href="{{ path('app_manga_index') }}">Manga</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white fw-semibold" href="{{ path('app_tag_index') }}">Tags</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-white fw-semibold" href="{{ path('app_category_index') }}">Categorie</a>
            </li>

        </ul>

        <!-- Liens à droite -->
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link text-white fw-semibold" href="{{ path('app_logout') }}">
                        {{ app.user.email }}
                    </a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="btn btn-outline-light me-2" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-light" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

<!-- Ajout des icônes Bootstrap -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
