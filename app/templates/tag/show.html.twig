{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center mb-4 text-white p-3 rounded" style="background: linear-gradient(135deg, #4caf50, #8bc34a);">
            🔖 Détails du Tag
        </h1>

        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ tag.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ tag.name }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="mt-3">
            <a class="btn btn-secondary" href="{{ path('app_tag_index') }}">
                ↩️ Retour à la liste
            </a>
            <a class="btn btn-warning ml-2" href="{{ path('app_tag_edit', {'id': tag.id}) }}">
                ✏️ Modifier
            </a>
        </div>

        {{ include('tag/_delete_form.html.twig') }}
    </div>
{% endblock %}
