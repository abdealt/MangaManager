{{ form_start(form) }}
    <div class="mb-3">
        {{ form_widget(form) }}
    </div>

    <button type="submit" class="btn btn-primary shadow-sm">
        💾 {{ button_label|default('Sauvegarder') }}
    </button>
{{ form_end(form) }}
